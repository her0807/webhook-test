name: 'get-variables'
description: 'Get variables'

inputs:
  slack_url:
    required: true

outputs:
  lang:
    description: 'First value'
  service:
    description: 'Second value'

runs:
  using: 'node18'
  main: 'action.js'
